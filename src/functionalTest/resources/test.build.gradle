buildscript {
    repositories {
        mavenLocal()
    }
    dependencies {
        classpath("lazy.zoo.gradle:git-data-plugin:1.1.0")
    }
}

plugins {
    id("lazy.zoo.gradle.git-data-plugin")
}
