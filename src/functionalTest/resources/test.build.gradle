buildscript {
    repositories {
        mavenLocal()
    }
    dependencies {
        classpath("lazy.zoo.gradle:git-data-plugin:1.3.0")
    }
}

plugins {
    id("lazy.zoo.gradle.git-data-plugin")
}
